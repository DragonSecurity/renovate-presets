{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "For repositories that generate other projects. Files under a generator's template root are scaffold: they belong to the project they are generated into, not to the repository holding them. That cuts both ways, so this preset does two opposite things.",
  "customManagers": [
    {
      "description": "Workflow templates carry a .tmpl suffix, so the github-actions manager never matches them and their action versions are frozen at whatever was current when someone last edited them by hand. Generated projects then ship workflows that stop running -- the 2026 action majors are Node 20 to Node 24 migrations and Node 20 is being retired -- rather than workflows that merely lag.",
      "customType": "regex",
      "managerFilePatterns": ["/\\.github/workflows/.*\\.ya?ml\\.tmpl$/"],
      "matchStrings": ["uses:\\s+(?<depName>[\\w.-]+/[\\w.-]+)(?:/[\\w.-]+)*@(?<currentValue>\\S+)"],
      "datasourceTemplate": "github-tags"
    }
  ],
  "packageRules": [
    {
      "description": "Base images in a template must stay on their floating tag. A digest pinned here is baked into every project generated afterwards, which cannot update it without regenerating, so a base-image CVE never reaches them; the floating tag is picked up on their next rebuild. Scoped to the dockerfile manager so it does not also switch off the workflow templates above, which need the opposite treatment.",
      "matchFileNames": ["internal/generator/templates/**"],
      "matchManagers": ["dockerfile"],
      "enabled": false
    },
    {
      "description": "The same argument against pinning a base image applies to pinning an action digest into a generated project: a floating major tag keeps receiving patches wherever the project ends up, a digest does not. Stated explicitly because the org default extends helpers:pinGitHubActionDigests.",
      "matchManagers": ["custom.regex"],
      "matchFileNames": ["**/.github/workflows/*.tmpl"],
      "pinDigests": false
    }
  ]
}
